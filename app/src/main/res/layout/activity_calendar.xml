<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Calendar View -->
    <android.widget.CalendarView
        android:id="@+id/calendarView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"/>

    <!-- Text for selected date -->
    <TextView
        android:id="@+id/selectedDateTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Δεσμέυσεις Ημέρας"
        android:textSize="18sp"
        android:padding="10dp"
        android:textAlignment="center"
        android:layout_below="@id/calendarView" />

    <!-- RecyclerView to display reservations -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/reservationsRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_below="@id/selectedDateTextView"
        android:layout_above="@id/addReservationButton"
        android:layout_alignParentStart="true"/>

    <!-- Button to add reservations (placed at the bottom-right corner) -->
    <ImageButton
        android:id="@+id/addReservationButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_margin="16dp"
        android:contentDescription="Add Reservation"
        android:src="@android:drawable/ic_input_add" /> <!-- Material icon for add button -->

</RelativeLayout>
