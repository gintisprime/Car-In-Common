<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <ImageView
        android:id="@+id/imageViewProfilePicture"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/ic_launcher_background"
        android:layout_gravity="center"
         android:layout_marginBottom="16dp"
        android:contentDescription="Προφίλ εικόνας" />


    <Button
        android:id="@+id/buttonUploadPicture"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Εισαγωγή εικόνας"
        android:layout_gravity="center"
        android:layout_marginBottom="16dp" />

    <!-- Text Fields -->
    <EditText
        android:id="@+id/editTextTeamName"
        android:hint="Όνομα ομάδας"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp" />

    <EditText
        android:id="@+id/editTextCarModel"
        android:hint="Μοντέλο αυτοκινήτου"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp" />

    <EditText
        android:id="@+id/editTextCarPlate"
        android:hint="Αριθμός πινακίδας"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp" />

    <!-- Submit Button -->
    <Button
        android:id="@+id/buttonSubmitCarDetails"
        android:text="Είσοδος"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp" />
</LinearLayout>
